/**
 * Created by yinwk on 2017/7/17.
 */
//Figure组件图片style内联样式表设置并导出
export function styleConfig(src) {
    return {
        background: "url(" + src + ") no-repeat center center/cover border-box content-box"
    }
}